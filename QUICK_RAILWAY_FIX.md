# 🚀 Быстрое решение проблемы деплоя на Railway

## Проблема: "There was an error deploying from source"

### ✅ Решение:

**Главная причина** - Railway не знает, где находится корень проекта для каждого микросервиса.

### Шаги для исправления:

1. **Откройте настройки сервиса в Railway**
   - Перейдите в ваш проект на Railway
   - Выберите сервис (например, `movie-service`)
   - Нажмите на вкладку **Settings**

2. **Настройте Root Directory:**
   - В разделе **Source** найдите поле **Root Directory**
   - Введите название папки микросервиса:
     - `movie-service` для movie-service
     - `user-service` для user-service
     - `rating-service` для rating-service
     - `api-gateway` для api-gateway
     - `eureka-server` для eureka-server
   - **Сохраните изменения** (Save)

3. **Проверьте команды сборки:**
   - В разделе **Deploy** проверьте:
     - **Build Command**: `mvn clean package -DskipTests`
     - **Start Command**: должен автоматически определиться из `Procfile`

4. **Перезапустите деплой:**
   - Нажмите **Redeploy** или дождитесь автоматического перезапуска

### 📁 Структура файлов в каждом микросервисе:

Каждый микросервис теперь содержит:
- ✅ `pom.xml` - конфигурация Maven
- ✅ `Procfile` - команда запуска для Railway
- ✅ `railway.json` - конфигурация Railway (опционально)
- ✅ `src/main/resources/application.yml` - конфигурация Spring Boot с поддержкой Railway переменных

### ⚠️ Важно:

- **Root Directory** должен быть указан для КАЖДОГО микросервиса отдельно
- Не используйте корень репозитория (`/`) - это вызовет ошибку
- Убедитесь, что в папке микросервиса есть `pom.xml`

### 🔍 Проверка:

После настройки Root Directory, Railway должен:
1. Найти `pom.xml` в указанной папке
2. Выполнить сборку: `mvn clean package -DskipTests`
3. Найти JAR файл в `target/`
4. Запустить приложение используя `Procfile`

Если проблема сохраняется, проверьте логи сборки в Railway для выявления конкретной ошибки.

